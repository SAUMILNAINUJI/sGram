<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Premium | sGram</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">


    <link rel="stylesheet" href="/stylesheets/navbar.css">
    <link rel="stylesheet" href="/stylesheets/flash.css">

    <style>
        /* --- Custom Button Styles --- */

        /* Reusing btn-edit-theme for the main purchase button */
        .btn-purchase-theme {
            background-color: var(--edit-color);
            border-color: var(--edit-color);
            color: white;
            transition: all 0.2s ease-in-out;
            font-weight: 600;
        }

        .btn-purchase-theme:hover {
            background-color: #E67E22;
            border-color: #E67E22;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(243, 156, 18, 0.4);
        }

        /* Premium Button (Golden) */
        .btn-premium-theme {
            background-color: var(--premium-color);
            border-color: var(--premium-color);
            color: var(--secondary-color) !important;
            font-weight: 700;
            padding: 5px 15px;
            margin: 4px 4px;
            border-radius: 5px;
            transition: background-color 0.2s;
        }

        .btn-premium-theme:hover {
            background-color: #E7B400;
            border-color: #E7B400;
            color: var(--secondary-color) !important;
        }

        /* --- Layout & Card Styles --- */
        .premium-card-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: calc(100vh - 70px);
        }

        .premium-card {
            max-width: 500px;
            width: 100%;
            padding: 40px;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            background-color: white;
            color: var(--secondary-color);
            /* NEW: Subtle glow for the premium focus */
            border: 1px solid rgba(243, 156, 18, 0.3);
        }

        .premium-card:hover {
            box-shadow: 0 12px 35px rgba(243, 156, 18, 0.2);
        }

        .feature-list {
            list-style: none;
            padding-left: 0;
            text-align: left;
        }

        .feature-list li {
            margin-bottom: 12px;
            font-size: 1.05rem;
            color: var(--secondary-color);
        }

        .feature-list i {
            color: var(--success-color);
            margin-right: 10px;
            font-size: 1.1em;
        }

        /* NEW: Custom Pricing Tag */
        .price-tag {
            display: inline-block;
            padding: 10px 20px;
            background-color: var(--primary-color);
            color: white;
            border-radius: 8px;
            font-size: 1.5rem;
            font-weight: 700;
            box-shadow: 0 4px 10px rgba(52, 152, 219, 0.3);
            margin-top: 15px;
            margin-bottom: 25px;
        }
    </style>
</head>

<body>
    <%- include('partials/navbar', {activePage: 'profile' }) %>
    <%- include('partials/flash') %>   
        <div class="premium-card-container">
            <div class="premium-card text-center">

                <h2 class="mb-3" style="color: var(--secondary-color);">
                    <i class="fas fa-crown me-2" style="color: var(--edit-color);"></i> Upgrade to Premium
                </h2>
                <p class="lead text-muted mb-4">Unlock the full potential of your Photo Gallery today.</p>

                <ul class="feature-list my-4">
                    <li><i class="fas fa-check-circle"></i> <strong style="color: maroon;">Unlimited</strong> photo
                        uploads
                    </li>
                    <li><i class="fas fa-check-circle"></i> <strong style="color: maroon;">Faster </strong>image
                        processing
                        & viewing</li>
                    <li><i class="fas fa-check-circle"></i> <strong style="color: maroon;">Ad-free </strong> experience
                        across the platform</li>
                    <li><i class="fas fa-check-circle"></i> <strong style="color: maroon;">Priority </strong>customer
                        support</li>
                </ul>

                <div class="price-tag">
                    One-time payment: **₹99**
                </div>

                <button id="payBtn" class="btn btn-lg btn-purchase-theme w-100">
                    <i class="fas fa-shopping-cart me-2"></i> Buy Premium Access
                </button>
                <p class="small text-muted mt-2">Secure payment via Razorpay.</p>

            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <script>
            document.getElementById("payBtn").addEventListener("click", async () => {
                // Disable button and show spinner to prevent multiple clicks
                const payBtn = document.getElementById("payBtn");
                const originalHTML = payBtn.innerHTML;

                payBtn.disabled = true;
                payBtn.innerHTML = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span> Loading...';

                try {
                    // 1. Create Order ID from server
                    const res = await fetch("/create-order", { method: "POST" });
                    if (!res.ok) throw new Error('Failed to create order.');

                    const order = await res.json();

                    if (!order || !order.id) throw new Error('Invalid order response from server.');

                    const options = {
                        // !!! REMINDER: Replace with your actual key ID when deploying !!!
                        key: "rzp_test_RhVYKPOupv38C4",
                        amount: order.amount,
                        currency: order.currency,
                        name: "Photo Gallery Premium",
                        description: "Unlimited photo uploads",
                        order_id: order.id,
                        handler: async function (response) {
                            // 2. Payment success handler → Verify it on server
                            const verificationRes = await fetch("/verify-payment", {
                                method: "POST",
                                headers: { "Content-Type": "application/json" },
                                body: JSON.stringify(response)
                            });

                            if (!verificationRes.ok) {
                                alert("Payment successful but verification failed. Please contact support.");
                                // Re-enable button on failure to retry
                                payBtn.disabled = false;
                                payBtn.innerHTML = originalHTML;
                                return;
                            }

                            // 3. Final success and redirect
                            alert("Payment successful! You are now a Premium user.");
                            window.location.href = "/dashborad?success=Premium access activated successfully!";
                        },
                        modal: {
                            ondismiss: function () {
                                // Re-enable button if modal is closed without payment
                                payBtn.disabled = false;
                                payBtn.innerHTML = originalHTML;
                            }
                        }
                    };

                    const rzp = new Razorpay(options);
                    rzp.open();

                } catch (error) {
                    console.error("Payment initiation failed:", error);
                    alert("Could not start payment. Please try again.");
                    // Re-enable button on error
                    payBtn.disabled = false;
                    payBtn.innerHTML = originalHTML;
                }


            });

            document.addEventListener('DOMContentLoaded', function () {

                // --- Auto hide success alert after 3 seconds ---
                const successAlert = document.querySelector('.alert-success');
                if (successAlert) {
                    setTimeout(() => {
                        // Use bootstrap methods for cleaner alert dismissal
                        const bsAlert = bootstrap.Alert.getOrCreateInstance(successAlert);
                        bsAlert.close();
                    }, 3000);
                }
            });
        </script>
</body>

</html>